<!-- Navbar -->
<nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="{{ path('product_index') }}" class="nav-link">Home</a>
        </li>
    </ul>




    <!-- Right navbar links -->
    <ul class="navbar-nav ml-auto">
        {% if is_granted('ROLE_owner') %}
            <li><a class="btn btn-primary" id="logout" href="{{ path('security_logout') }}"  title="Logout">Logout</a></li>
        {% elseif is_granted('ROLE_Admin') %}
            <li><a class="btn btn-primary" id="logout" href="{{ path('security_logout') }}"  title="Logout">Logout</a></li>
        {% elseif is_granted('ROLE_customer') %}
            <li><a class="btn btn-primary mr-2" id="logout" href="{{ path('country_new') }}"  title="Logout">convert to owner shop</a></li>
            <li><a class="btn btn-primary" id="logout" href="{{ path('security_logout') }}"  title="Logout">Logout</a></li>
        {% else  %}
            <li><a class="btn btn-primary" id="logout" href="{{ path('security_login') }}"  title="Logout">Sign In</a></li>
        {% endif %}

        <li class="nav-item">
            <a class="nav-link" data-widget="control-sidebar" data-slide="true" href="#">
                <i class="fas fa-th-large"></i>
            </a>
        </li>
    </ul>
</nav>
<!-- /.navbar -->


